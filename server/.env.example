# Copia questo file in server/.env (mai committato) per test da terminale senza passare
# dalla UI. Non obbligatorio: il frontend può sempre inviare la propria chiave a ogni
# richiesta invece di affidarsi a questa.
PORT=8787
GEMINI_API_KEY=
