{
  "name": "aurora-markets-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend locale Aurora Markets: server+client MCP in-process che orchestra i 7 agenti e il provider AI principale. Non un backend di produzione — nessun broker, nessun ordine reale.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^5.2.1",
    "zod": "^3.24.1",
    "dotenv": "^16.4.5"
  }
}
